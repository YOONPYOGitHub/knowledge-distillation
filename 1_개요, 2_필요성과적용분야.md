# 1. 프로젝트 개요

## 1.1 연구 주제
본 프로젝트의 연구 주제는 **지식 증류(Knowledge Distillation) 기반 경량 언어모델**이다.
핵심 질문은 다음과 같다.

> 대형 언어모델이 가진 성능을, 어디까지 작은 모델에서 보존할 수 있는가.

이를 위해 Teacher Model의 출력 분포·응답 특성·추론 능력을 Student Model이 학습하도록 하는 지식 증류를 중심에 두고, 필요에 따라 프루닝·양자화 등 추가 압축 기법과의 결합을 검토한다.

## 1.2 연구의 범위 설정 — 특정 목적을 갖는 모델의 경량화
경량화는 그 자체로 목적이 아니며, 본 프로젝트는 그 범위를 다음 두 가지 판단 위에서 설정한다.

**(1) SOTA 경량화가 아니라 특정 목적 모델의 경량화에 집중한다.**
모델 경량화는 이미 학계와 기업에서 다양한 연구가 활발히 진행되고 있는 분야이며, 단기간의 프로젝트로 그 최신(SOTA) 수준을 따라잡는 것은 현실적으로 어렵다고 판단하였다. 따라서 범용 SOTA 모델의 경량화를 목표로 삼기보다는, **명확한 사용 목적을 갖는 모델의 경량화**에 집중하는 것이 한정된 기간 안에서 더 큰 가치와 의미를 가진다고 보았다.

**(2) 경량화의 가치는 제약 환경에서 발생한다.**
네트워크와 연산 자원이 충분한 환경에서는, 프로젝트 규모의 경량화 결과는 실효성이 낮다. 대형 모델을 그대로 쓰거나 클라우드 추론으로 대체하면 되기 때문이다. 경량화가 실질적 가치를 갖는 지점은 **작은 모델이 "선택이 아니라 요건"이 되는 제약 환경**이다.

이 두 판단을 종합하면, 연구는 다음 두 제약을 동시에 만족하는 환경에 정박해야 한다.

1. **연결성·프라이버시 제약** — 안정적 네트워크를 전제할 수 없고, 민감 데이터의 외부 전송을 피해야 함
2. **연산 자원 제약** — 고성능 서버·대형 모델을 직접 사용하기 어렵고, 디바이스의 제한된 자원만으로 처리해야 함

## 1.3 적용 시나리오 — 실버케어 온디바이스 AI
위 두 제약을 가장 잘 충족하는 사례로 **실버케어 온디바이스 AI**를 적용·검증 시나리오로 선정하였다.

## 1.4 연구 본체와 적용 분야의 관계
| 구분 | 내용 | 위상 |
| --- | --- | --- |
| **연구 본체** | 지식 증류 기반 경량 언어모델 | 본 프로젝트가 검증하려는 대상 |
| **적용 분야** | 실버케어 온디바이스 AI | 경량화 결과의 가치를 설명·검증하는 대표 시나리오 |

즉, 실버케어는 프로젝트의 출발점이 아니라, **경량화 연구를 의미 있게 만들고 그 결과를 검증하기 위해 의도적으로 선택한 제약 환경**이다.

---

# 2. 연구 필요성 및 적용 분야 선정 배경

## 2.1 경량 언어모델 연구의 필요성
대형 언어모델은 높은 성능을 보이지만, 실제 서비스 환경에서는 다음 한계를 가진다.

- 모델 크기가 커서 일반 디바이스에 탑재하기 어려움
- 추론 비용과 연산 자원 요구가 큼
- 동작에 네트워크 연결이 필요한 경우가 많음
- 실시간 응답 환경에서 지연 시간이 문제가 될 수 있음
- 민감 데이터를 서버로 전송해야 하는 경우 개인정보 이슈가 발생함

따라서 대형 모델의 성능을 최대한 유지하면서도 작은 모델로 동작 가능한 **경량화 연구**가 필요하다.

## 2.2 경량화의 가치 조건 — 제약이 없으면 의미가 없다
경량화의 필요성은 환경에 따라 달라진다.

- **네트워크가 원활하고 연산 자원이 충분한 환경**에서는 경량화의 실익이 작다. 대형 모델 또는 클라우드 추론으로 대체할 수 있기 때문이다.
- 경량화가 실질적 가치를 갖는 지점은 **작은 모델이 불가피한 제약 환경** — 즉 *오프라인·온디바이스에서, 제한된 자원으로, 외부 전송 없이* 동작해야 하는 환경이다.

이로부터 적용 분야 선정의 핵심 기준이 도출된다.
**"경량화가 선택이 아니라 요건이 되는 환경인가"** 가 그것이다.

## 2.3 적용 시나리오 선정 기준과 후보 검토
위 논의를 정리하면, 적용 분야는 다음 두 제약을 동시에 만족해야 한다.

1. **연결성·프라이버시 제약**: 네트워크를 전제할 수 없고, 데이터가 디바이스 외부로 나가지 않아야 함 → 온디바이스 처리가 요건
2. **연산 자원 제약**: 고성능 하드웨어를 전제할 수 없어, 제한된 디바이스 자원만으로 처리해야 함 → 경량 모델이 요건

이 기준에 비추어 다음과 같은 후보들을 함께 검토하였다.

- **네트워크 환경이 열악한 건설 현장에서 사용되는 단말**
- **차량 내 승객의 지시사항을 처리하기 위한 디바이스**
- **AI 제세동기(또는 스마트 메디컬 디바이스)**
- **어린이 교육용 디바이스**

각 후보는 두 제약 중 일부를 만족하나, 두 제약을 *동시에, 그리고 강하게* 만족하는 정도에서 차이가 있었다. 이를 종합적으로 비교한 결과, **실버케어 온디바이스 AI**를 대표 적용 시나리오로 선정하였다.

## 2.4 실버케어 온디바이스 AI를 선정한 이유
**실버케어 온디바이스 환경은 위 두 제약을 모두 강하게 만족한다.**

**(1) 연결성·프라이버시 제약 충족**
- 독거노인 등 가정에는 안정적 네트워크 인프라가 갖춰져 있지 않은 경우가 많음 → 오프라인 동작 필요
- 건강·생활 데이터 등 민감 정보를 다루므로, 외부 전송을 피하고 디바이스 내 처리(온디바이스)가 요구됨

**(2) 연산 자원 제약 충족**
- 고성능 서버나 대형 디바이스 사용이 어려운 환경
- 따라서 제한된 디바이스 자원만으로 사용자의 요청을 처리해야 함

또한 실버케어는 응답의 **안전성·민감 질의 처리**가 중요한 도메인이어서, 단순 경량화 성능을 넘어 적용 단계에서 요구되는 평가 관점까지 자연스럽게 함께 도출할 수 있는 사례이기도 하다.

## 2.5 정리 — 연구의 본체와 검증 환경
- 본 프로젝트의 **연구 본체는 지식 증류 기반 경량 언어모델**이다.
- 실버케어 온디바이스 AI는, **경량화가 요건이 되는 두 제약을 가장 잘 충족하는 대표 환경**으로서, 연구 결과의 가치를 설명하고 검증하기 위한 적용 시나리오로 선정되었다.
