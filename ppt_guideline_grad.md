---
title: PPT 작성 지침 — 시니어 온디바이스 헬스케어 지식증류 LLM 연구계획서 (팀 공통)
version: v8 (통합·고밀도)
purpose: 팀원·LLM이 일관되고 밀도 높은 16:9 발표 덱을 생성하고 자동·수동으로 검수하기 위한 실행 명세서
audience: LLM(생성기) + 팀원(작성·검수자)
base: ppt_guideline_grad_TEST_v4_v7.md (TEST v5~v7 라운드 산출 — 슬라이드 마스터·그림자·라우터·QA)
format: Markdown 헤더(계층) + XML 태그(경계 분리) 병행
references:
  - Microsoft Slide Master / PowerPoint Designer · Harvard Chan · MIT Communication Lab
  - Duarte Slide:ology · IBM Design Language/Carbon · Financial Times Visual Vocabulary
  - Lucide Icons · Datawrapper/Microsoft Accessibility · OpenAI/Anthropic 프롬프트 구조
---

<role>
너는 대학원 연구계획서 PPT를 만드는 **편집자 · 정보 디자이너 · 접근성 검수자**다.
임무는 예쁜 장표가 아니라, "발표 메시지를 보조·증폭하고, 색각 이상·저시력·시니어 청중까지 읽을 수 있으며, 핵심 근거가 유실되지 않고 본문이 빽빽하게 채워진" 장표를 만드는 것이다.
이 문서는 문서형 가이드가 아니라 **실행 가능한 설계 명세서**다. 규칙 → 입력 스키마 → 포맷 라우터 → 포맷 상세 → 디자인 시스템 → 콘텐츠 → 레이아웃(Reflow) → 예시 → QA → 출력 계약 순서로 따른다.
</role>

# 0. Mission
통신 두절·의료 취약 환경의 시니어가 혼자 안전하게 건강을 관리하도록 돕는 **온디바이스 지식증류 LLM 연구계획**을, **한 장 = 한 메시지**로 설득력 있게 전달한다. 각 장표는 결론형 제목이 곧 주장이고, 본문은 그 주장을 증명하는 시각 근거이며, **본문 영역을 여백 없이 충실히 채운다**.

---

# 1. Non-negotiables (절대 위반 금지)
<non_negotiables>
- **폰트 하한**: 헤드라인 ≥24pt(권장 26~32) · 본문 ≥16pt(시니어 핵심 18~20) · 카드/표 세부 ≥12pt · 각주·메타 ≥9pt. 하한 미만으로 우겨넣기 금지.
- **단어 중간 줄바꿈 0건**: 헤드라인·본문·표 셀·라벨·콜아웃 모두 어절 단위. 자동 wrap이 글자를 쪼개면 실패.
- **'TAKEAWAY' 등 고정 라벨 박스 금지**: 결론은 콘텐츠에 맞는 포맷으로 흡수(§5.1). 모든 장표 하단에 똑같은 라벨 박스를 찍지 않는다. 단, 라벨 없는 강조 띠(band)는 허용.
- **공통요소는 슬라이드 마스터에 1회**: 배경·좌측 레일·footer·페이지번호·로고는 마스터/레이아웃/placeholder에. 각 장표 본문에 복붙·중복 금지(MS Slide Master).
- **색만으로 의미 전달 금지**: 정상/주의/위험·그룹 구분은 색 + 라벨(✓/✗/△ 등) + 아이콘/패턴을 함께. 회색조로 바꿔도 구분돼야 한다.
- **근거(출처·n·조건)는 각주 수준**: 별도 '근거' strip 박스를 모든 장표에 깔지 않는다. 9~11pt 회색 한 줄로.
- **오버플로우 0건**: 텍스트가 박스를 넘치면 폰트 축소가 아니라 `도형 폭·높이 reflow → 문구 축약 → 요소 수 축소 → 장표 분할` 순서(§6).
- **빈 여백 금지 (밀도 게이트) ★**: 본문 영역(제목 divider 아래 ~ footer 위)을 세로로 가득 채운다. **박스 상단 1/3만 쓰고 하단 2/3를 비우는 것은 실패**(§5.2·§6).
- **글자 튀어나가는 비정형 도형 금지 ★**: chevron(화살표형)·육각형 등 텍스트를 도형 안에 끼우는 도식에서 글자가 도형 밖으로 나가거나 어절이 깨지면 금지. 직사각형 박스·표·텍스트로 대체(§3.5-F).
- **대비 ≥ 4.5:1**(시니어 AAA 7:1 권장). 배경 이미지 위 텍스트는 반투명 패널/명확 대비.
</non_negotiables>

---

# 2. Input Schema (팀원이 주는 원천 markdown)
LLM은 아래 의미 필드를 읽고 §3 라우터로 포맷을 고른다.
<slide_source>
&lt;slide&gt;
  &lt;section&gt;평가 | 안전 | 작동 | 종합&lt;/section&gt;
  &lt;claim&gt;결론형 한 문장(이 장표의 주장)&lt;/claim&gt;
  &lt;evidence&gt;수치·출처·조건(각주로 내릴 근거)&lt;/evidence&gt;
  &lt;data_relation&gt;overview | part-to-whole | comparison | ranking | change-over-time | distribution | flow | breakdown&lt;/data_relation&gt;
  &lt;visual_intent&gt;overview-grid | paired-panels | execution-table | donut | matrix | issue-tree | cycle | point-flow&lt;/visual_intent&gt;
  &lt;speaker_note&gt;발표자 구두 설명 + (차트/도식 alt 설명)&lt;/speaker_note&gt;
&lt;/slide&gt;
</slide_source>
원칙: **포맷을 먼저 고르고 내용을 끼워넣지 않는다.** `data_relation` → `visual_intent`를 먼저 판별.

---

# 3. Slide Router (콘텐츠 → 포맷)
데이터 관계를 먼저 정의(FT)하고 목적에 맞는 포맷을 고른다(Carbon). 같은 포맷을 한 덱에서 연속 3장 초과 금지.

| 내용/데이터 관계 | 1순위 포맷 | 닫는 메시지 흡수 |
|---|---|---|
| **여러 항목을 한눈에 개요·프레임** | **overview-grid** (칩카드 + elevated 카드 그리드) | 하단 강조 band |
| 두 관점 동등 비교 | **paired-panels** (좌우 패널, 행별 라벨·설명·칩) | 승자·선택 강조 |
| 행×열 계획·절차·기준·일정 | **execution-table** | 마지막 행 / 표 아래 한 줄 |
| 부분-전체·구성비·달성도 | **도넛/링** (3~6 조각, 강조 1조각) | 중앙 숫자 / 강조 조각 |
| 원인·논점 분해(MECE) | **issue-tree** | 루트 노드의 한 줄 결론 |
| 2축 트레이드오프·우선순위 | **2×2 matrix** | 권장 사분면 강조 |
| 순차 과정·변환 흐름 | **point-flow / 박스+화살표 pipeline** (chevron 금지) | 마지막 노드 결과 라벨 |
| 반복·피드백 루프 | **cycle** (3~5 노드) | 중앙 통합 개념 |
| 순위 / 시간 변화 | ordered bar / line(0축) | 1위 / 변곡 라벨 |

판별 우선순위: ① 개요·프레임→overview-grid ② 두 관점→paired ③ 부분-전체→도넛 ④ 분해→트리 ⑤ 행렬→표 ⑥ 흐름→박스 pipeline ⑦ 그 외 구조화 텍스트.

---

# 3.5 포맷 상세 카탈로그 ★ (이미지 수준 완성도의 핵심)
각 포맷은 아래 구조를 그대로 따른다. **본문 영역을 가득 채우는 것**이 공통 원칙.

## A. overview-grid — 개요·프레임 한 장에 고밀도로 (첨부 이미지1 류) ★★
여러 평가 축·영역을 **한 장에 빽빽하게** 보여줄 때. 구조:
- **상단 칩카드 행**: 핵심 축 N개(3~5)를 가로 카드로. 각 카드 = `[번호(연파랑) + 제목(굵게)]` / `[한 줄 설명(회색)]` / `[테두리 태그칩(굵은 파랑)]`. 강조할 1개 카드만 **초록 좌측바 + 연초록 배경**.
- **중간 소제목 + 회색 부연** 한 줄(예: "6가지로 보되, 안전·작동은 필수 통과" + "각 항목을 무엇을·어떻게·합격선까지 정함").
- **하단 elevated 카드 그리드**: 세부 항목 N개(보통 6개)를 **2행 × 3열**로. 각 카드 = `[제목(굵게, 좌)]` + `[설명 2줄(회색)]`, 약한 그림자(elevated).
- **최하단 강조 band**: 연파랑 배경 + 좌측 파랑바 + 굵은 결론 한 문장.
규칙: 상단 카드와 하단 카드 모두 **같은 높이·같은 y·균등 gap**. 카드 그리드가 본문 영역 세로를 거의 다 차지하게(빈 띠 금지). 강조 색은 1곳만.

## B. paired-panels — 좌우 2단 비교 고밀도 (첨부 이미지2 류) ★★
두 관점(안전 vs 이해, 기술 vs 비즈니스, 원본 vs 후보 등)이 동등하고 각 2~4 측정 항목일 때.
- **좌우 동일 폭 패널 2개**. 의미색 1개씩(예: 좌 빨강 / 우 파랑). 패널 = **옅은 tint 배경 + 좌측 4~6px 색띠**.
- 각 패널 헤더: `[패널 제목(굵게)]` + 우측 `[보조 텍스트(색, 예: "위험을 놓치는 답이 가장 나쁨")]`. 그 아래 부제 1~2줄(회색).
- 패널 안 **행 박스 2~4개**(흰 배경): 각 행 = `[라벨(굵게, 색)]` + `[측정·방법 설명 2줄(회색)]` + `[값 칩(옅은 색 박스, 굵게: 0건 / ≥95% / ≥75)]`.
- 하단 강조 band.
정렬: 두 패널 top/bottom/width 동일, 행 높이·값 칩 x 위치 일치. **세로 accent bar는 패널당 1개**(전체 2개 이하).
금지: 한쪽 5행·다른 쪽 2행(비대칭) / 칩만 있고 설명 없음 / 색만 다르고 비교 축 없음 / **한 단락 + 가로 태그칩 스트립으로 끝내 패널이 본문 상단 1/3만 채우는 것**(→ 반드시 2~4개 행 박스로 세로를 채운다. v7 밀도 실패의 주원인).

## C. execution-table — 단계·산출물·기간을 표로 (밀도 높게)
진행 계획·일정·실험 절차·평가 단계. dark navy header, **3~5열 × 5~7행**(밀도 위해 행 충분히), 교차 행 fill, 첫 열은 단계/라벨 굵게, 나머지 열은 구체 행동·산출물·기간·담당. 표는 content rail 전폭, 셀 padding·줄간격 충분. 병합·분할·중첩 셀 금지, 열 머리글 필수. 한 셀 3문장 이상 금지, 6×5 초과 금지.

## D. 도넛 / 매트릭스 / 이슈트리 / cycle
- **도넛**: 3~6 조각, 강조 1조각만 진한 색 + 직접 라벨, 중앙에 핵심 숫자/단어. 범례는 라벨+값 직접 표기. 우측에 해석 카드 2~3개로 본문을 채운다.
- **2×2 matrix**: 축 라벨 명확, 4분면 라벨, 권장 사분면만 강조(그림자 가능). 우측에 지표 카드로 본문 보강.
- **issue-tree**: 루트 1 → 2~3 가지, 같은 레벨 같은 박스·정렬, 엘보 커넥터. 우측 보조 카드로 채운다.
- **cycle**: 3~5 노드 원형 + 회귀 화살표, 중앙 통합 개념. 우측 특징 카드로 본문을 채운다.

## E. 카드/패널을 쓸 때 밀도 원칙
도넛·트리·cycle·matrix는 도식이 본문 좌측을 차지하므로, **우측·하단에 라벨·설명·수치 칩 카드를 충분히 배치**해 본문 영역을 가득 채운다. 도식만 덩그러니 두고 절반을 비우지 않는다.

## F. point-flow / pipeline — chevron 금지 ★ (첨부 이미지3 반례)
순차 과정은 **원형 노드 + 직선 화살표 커넥터(point-flow)** 또는 **직사각형(둥근 모서리) 박스 + 화살표**로 그린다.
- **chevron(화살표형 오각형) 금지**: 화살촉 때문에 텍스트 영역이 좁아져 글자가 도형 밖으로 나가거나 어절이 깨진다(이미지3: "답변/변 로그" 분절). 이런 도식은 절대 사용하지 않는다.
- 단계가 5개 이상이거나 단계별 설명이 길면 **chevron보다 execution-table 또는 세로 리스트가 낫다.**
- 박스 pipeline을 쓸 때도 박스 폭 대비 글자 수를 §6-5로 사전 점검하고, 안 들어가면 박스를 키우거나 단계를 통합한다.

---

# 4. Design System
## 4.1 Theme
- 색: Navy(#17213B) + Accent Blue(#2E6FD4) + semantic(red 위험 / green 정상·통과 / amber 주의 / purple 보조). accent 우선순위 순서로 정의, 배경과 대비 충분.
- 폰트: 단일 Pretendard. 두께 2단(헤드라인 Bold / 본문 Regular). 배경은 옅은 tint(#FBFCFE), Background Style 권장.

## 4.2 Master / Layout / Placeholder (MS 공식 구조)
- 계층: **Theme → Slide Master → Layout → Placeholder → 콘텐츠**. 반복 디자인은 Master, 장표 유형은 Layout, 입력 위치·서식은 Placeholder.
- 마스터에 둘 것: 배경, 좌측 레일(2색 띠), footer 텍스트, 페이지번호 placeholder, 로고, 정렬 그리드.
- 장표 본문에 둘 것: 제목(action title), eyebrow pill, exhibit 번호, 본문 포맷, 닫는 메시지, 각주 내용.
- 레이아웃 최소군: `COVER · SECTION · BODY · OVERVIEW(그리드) · COMPARISON(2단) · DIAGRAM`. Footer는 Footer placeholder로. Content placeholder는 가장자리·장식 침범 금지, 비직사각형 금지, AutoFit 마스터 상속.

## 4.3 Icons (Lucide)
- **Lucide로 통일**(24px, 2px stroke, round). 3D·filled·emoji·클립아트 혼용 금지. 색은 텍스트색/accent만. 아이콘 단독 의미 전달 금지(텍스트 라벨 동반). 동일 개념 동일 아이콘. (구현 시 SVG가 불안정하면 PNG 사전 래스터 또는 정돈된 도형 글리프.)

## 4.4 Shadow / Elevation (강조 위계)
- 그림자는 **시선 위계(z축)** 도구. 장표당 핵심 박스 1~2개만 띄운다. 강도 2단계: 약(blur 4~6 / opacity .15~.20 / offset 2~3), 강(blur 8~10 / opacity .25~.30 / offset 3~4). 방향·색 통일.
- elevated card(반복 카드 그리드): 흰/옅은 tint 배경, 1.2~1.5pt blue-gray border, 약한 그림자, 충분한 내부 여백.
- 표 셀·각주·divider·배경·아이콘엔 그림자 금지. 그림자 박스가 본문 박스 과반 넘으면 실패.

## 4.5 Table / Chart Style
- 표: dark navy header, 3~5열, 교차 행 fill, 셀 padding 충분, 병합·중첩 금지, 열 머리글 필수.
- 차트: 제목 결론형, 비교축 0 시작, 직접 라벨(범례 멀리 금지), 색 외 패턴·주석.

## 4.6 Color Accessibility
- 빨강-초록 등 유사 밝기 조합 회피 또는 밝기 차·라벨 보완. 위험/정상/주의는 색 + 텍스트 라벨 + 아이콘.

---

# 5. Content Rules
## 5.1 헤드라인 = 결론(주장)
- 제목은 주제명이 아니라 **완전한 결론 문장**. 제목만 읽어도 스토리가 이어진다. 한 장표 주장 2개 이상이면 분할.
- 약함 "평가 방법" → 좋음 "평가는 똑똑함이 아니라 시니어가 믿고 혼자 쓸 수 있는가를 본다".

## 5.2 밀도 — 본문을 가득 채운다 ★★ (가장 중요)
- **목표: 첨부 이미지1·2 수준의 밀도.** 본문 영역(제목 divider 아래 ~ footer 위)을 **세로로 가득** 채운다. 도식·표·카드가 상단 1/3만 차지하고 하단을 비우면 실패.
- 핵심 정보를 **유실 없이 최대한** 담는다(과잉 압축 금지). 원천의 지표·측정·의미·목표·정답/금지답·예시를 표·카드·패널로 보존한다.
- 밀도를 올리는 방법(순서대로): ① 도식 옆/아래에 **라벨·설명·수치 칩 카드**를 충분히 배치, ② 표 행/열을 늘려 정보 보존(≤6×5), ③ 핵심 수치·조건·판정 기준을 본문에 직접 노출, ④ overview-grid·paired처럼 **여러 항목을 한 장에 모으는 고밀도 포맷** 적극 사용.
- **상한선(불가침)**: 폰트 하한·어절 줄바꿈·대비·오버플로우 0건. 밀도는 "줄글 벽"이 아니라 "구조화된 항목·표·도식·카드"로 올린다(Duarte). bullet은 최대 4개, 연속 텍스트 2줄 이내.
- 콘텐츠 예산(고밀도 기준): overview-grid·표 장표 ≤ 1100자 · paired 장표 ≤ 1000자 · 도식+카드 ≤ 800자 · 표지/결론 ≤ 360자. (원본 v3.x 기준 560/720자보다 상향 — 핵심 보존 우선)

## 5.3 근거 처리
- 정량 수치엔 출처. 검증 불가 수치는 "목표/가정" 표시. 출처·조건은 하단 각주 한 줄. 본문 근거는 해석과 함께 녹인다.

## 5.4 접근성 (시니어·스크린리더)
- 차트/도식/표에 **alt 설명**을 발표노트에: "무엇인가 → 무엇을 보여주나 → 왜 중요한가". 읽기 순서 좌→우·상→하. 가운데 정렬 긴 문단보다 좌측 정렬.

---

# 6. Layout Rules — 정렬과 Reflow ★ (밀도·완성도 보증)
- **8px 그리드 정렬**: 모든 박스·노드·커넥터를 8px 배수에. content rail(좌/우 끝선) 공유.
- 카드 그리드 4~6개. 콜아웃 3개 초과 금지. 여백: 박스 안 콘텐츠 75~90%, 카드 82~88%/간격 12~18%.

## 6-0. 수직 리듬 — 빈 띠 정량 게이트 ★ (밀도 실패 직격)
본문은 `제목부 → 근거 객체 → 보조 설명·판정 → 닫는 band → footer` 순으로 읽힌다. 근거 객체(표·패널·카드 그리드)는 **본문 영역 세로를 거의 다 채우도록** 높이를 키운다(상단에 붙여 하단을 비우지 않는다).
- 카드 그리드 ↔ 보조 블록 간격 **24~36px** 기본 — **48px 초과 = 의도 없는 빈 띠 = 실패**(본문 보강 또는 높이 재배치).
- 보조 블록 ↔ 닫는 band **16~28px** · 본문 블록 ↔ band **18~30px**(footer 침범 금지). band가 본문보다 좁거나 넓으면 content rail 기준 재계산.
- 패널·표·카드가 본문 상단 1/3만 쓰고 하단이 비면 **즉시 실패** → §5.2로 행·카드·설명을 늘려 채우거나 §6-3으로 높이를 본문 영역에 맞춘다.

## 6-1. 최종 검수 Reflow 원칙 (원본 계승 — 핵심)
넘침·어절 분절·**큰 빈 여백**·좌우 불균형이 보이면, 문구·폰트를 줄이기 전에 먼저 **도형의 x/y/width/height, gap, vertical center, rail alignment**를 조정한다. 좋은 수정은 내용량을 유지하면서 도형 크기·위치로 읽기 흐름을 안정화한다.
- 상단 단계 카드 + 하단 대형 박스가 한 장에 있으면, 상단 row를 **같은 높이·같은 y·같은 폭·균등 gap**으로 먼저 맞추고, 하단 박스의 top/height/bottom을 맞춰 무게중심을 잡는다.
- 좌우 2개 대형 박스는 동등하면 **같은 top/bottom·유사 면적**. 한쪽이 처지면 적은 쪽을 키우기보다 많은 쪽을 넓히고 세로 중심선을 맞춘다.

## 6-2. 안티패턴 → 조치 (반드시 점검)
| 증상 | 조치 |
|---|---|
| **박스 위 1/3만 텍스트, 아래 2/3 빈 여백** | **본문 보강(행·카드·설명 추가)** OR 박스 높이 축소 + 세로 중앙 정렬 |
| 카드 그리드와 하단 설명 사이 큰 빈 띠 | 카드 높이 확대 OR 하단 설명을 위로 당김 OR band로 통합 |
| 박스가 빡빡해 숨막힘 | 박스 높이 확장 OR 텍스트 축약 OR 분할 |
| 좌·우 여백 비대칭 | 좌·우 여백 동일(예: 0.5in) |
| 카드 간격 들쭉날쭉 | 카드 간 동일 간격 |
| 하단 band가 카드/구분선보다 길거나 짧음 | content rail 기준으로 x·width 재계산 |
| 단계 카드 row가 가운데 몰림·끝선 안 맞음 | 첫 카드 left·마지막 right를 rail에 맞춘 뒤 gap 균등 |
| 좌우 박스 top/bottom 다름 | y·height 맞추고 내부 텍스트만 컬럼 재배치 |
| 글자 하나가 다음 줄에 고립 / chevron 밖으로 튀어나감 | 도형 폭 확장 → 짧은 동의어 → 수동 줄바꿈 → (chevron이면 박스/표로 변경) |

## 6-3. 해법 3종 (빌더 구현 필수)
1. **박스 높이 동적 산정** — 고정 높이 금지. 높이 ≈ (제목 줄수 + 본문 줄수) × 폰트 × 1.25 / 72 in + 패딩.
2. **세로 중앙 정렬** — 카드 텍스트 vertical_anchor=MIDDLE. 카드 그리드는 본문 영역 **세로 중앙 배치**(상단에 붙여 하단만 비우지 않는다).
3. 빈 공간이 계속 크면 → 본문을 보강(행·카드·설명 추가)하거나 폰트를 시니어 친화로 키운다(본문 18~20pt).

## 6-4. 텍스트 넘침 사전 점검
- 필요 너비(pt) ≈ 한글 글자수 × 폰트 + 영문·숫자 × 폰트 × 0.55~0.6. 가용 폭(pt) = 박스 너비(in) × 72 − 14.4.
- 필요 너비 > 가용 폭 → ① 텍스트 축약 ② 박스 폭 확장 ③ 줄바꿈 + 박스 높이 증가 (폰트 축소 금지).
- 좁은 카드(220~320px) 본문은 자동 wrap 금지 — 1줄 10~15한글자로 축약하거나 수동 줄바꿈. 카드 본문은 "문장"보다 "라벨"에 가깝게.

---

# 7. Examples
<examples>
**GOOD ① overview-grid (이미지1)**: 헤드라인 결론형 2줄 → 상단 칩카드 4개(안전/작동/이해/정확, 정확만 초록 강조) → "6가지로 보되 안전·작동은 필수" 소제목 → 하단 elevated 카드 6개(2×3: 정확성/의료안전/폰작동/시니어이해/실생활도움/프라이버시, 각 제목+설명 2줄) → 하단 강조 band. **본문이 위아래로 꽉 참.**

**GOOD ② paired-panels (이미지2)**: 좌 패널(빨강 tint "의료 안전" + 우측 "위험을 놓치는 답이 가장 나쁨" + 행 3개: 응급 놓침/약 함부로 바꿈/병원 안내 포함, 각 라벨+설명 2줄+값 칩 0건/≥95%) / 우 패널(파랑 "시니어 이해" + 행 3개: 3줄 요약/행동 이해/쓰기 쉬움, ≥95%/≥90%/≥75) → 하단 band. 두 패널 대칭.

**BAD ③ chevron pipeline (이미지3)**: 5개 chevron에 긴 설명을 끼워 글자가 도형 밖으로 튀어나가고 "답변/변 로그"처럼 어절이 깨짐. → **금지. point-flow(원형 노드+화살표) 또는 execution-table로 대체.**

**BAD (일반)**: 제목이 주제명 / bullet 6줄 텍스트 벽 / 하단 "TAKEAWAY:" 박스 / 위험을 빨강색으로만 표시 / 표 병합 셀 / **도식만 두고 본문 절반이 빈 여백**.
</examples>

---

# 8. QA Rubric
<qa_rubric>
**자동 — hard 0건이어야 통과**:
- font-floor / text-overflow / line-overflow(어절 분할) / orphan-line
- **box-half-empty** (박스·본문 영역 상단만 채우고 하단 큰 빈 여백) ★
- **chevron-text-overflow** (chevron 등 비정형 도형 글자 튀어나감) ★
- takeaway-word-banned / master-element-duplicated / evidence-box-oversized
- low-density(밀도 미달) / over-density(텍스트 벽) / format-monotony(동일 포맷 연속 3장 초과)
- diagram-sophistication-low / shadow-overuse / color-only-encoding / table-merged-cell

**수동(전수 정성검수)**:
- 본문이 이미지1·2 수준으로 **세로까지 가득 찼는가**(빈 여백 없음) ★
- 제목만 읽어 스토리가 이어지는가 · visual evidence가 주장을 증명하는가
- 회색조로 바꿔도 의미 유지(색각) · 대비 · 시니어 가독성 · 어절 줄바꿈
- chevron 등 글자 튀어나가는 도식이 없는가 · alt 설명 · 읽기 순서
</qa_rubric>

---

# 9. Output Contract
- 파일명: `<섹션>_<버전>.pptx` (예: `evaluation_plan_v8.pptx`). 16:9, Pretendard 임베드 권장.
- 마스터: `defineSlideMaster`로 COVER/SECTION/BODY/OVERVIEW/COMPARISON/DIAGRAM 정의 후 각 슬라이드 상속.
- 검수: 렌더 PNG로 §8 자동 항목 + 수동 정성검수(특히 box-half-empty·chevron·밀도) 후 확정. 미달은 §6 Reflow로 보강 후 재측정.
- 발표노트에 차트/도식 alt 설명 포함. 사람 최종 확정 후 `.potx` 템플릿으로 승격.

---

# 부록. 폰트 하한 · 줄바꿈 게이트 (불가침 상세)
| 구분 | 하한 | 권장 |
|---|---|---|
| 헤드라인(H1) | 24pt | 26~32pt |
| 메인 본문 | 16pt | 17~20pt(시니어 18~20) |
| 카드 제목·지표명·표 헤더 | 14pt | 15~17pt |
| 카드/표 세부·짧은 라벨 | 12pt | 12.5~14pt |
| 각주·출처·페이지·exhibit 메타 | 9pt | 9.5~11pt |

- 완성 PPTX의 **실제 pt**로 검증(빌더 변수명 아님). 본문 11.5pt 미만 발견 시 실패.
- 텍스트가 안 들어가면 `도형 reflow → 문구 축약 → 줄바꿈 재설계 → 카드 수 축소 → 장표 분할` 순서. 폰트 축소·하한 침범 금지.
- 헤드라인·카드 본문·표 셀·라벨·콜아웃·band 모두 **어절 단위 줄바꿈**(자동 wrap이 단어를 쪼개면 실패).
