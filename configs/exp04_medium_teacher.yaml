# exp04: gpt2-medium Teacher — Teacher 지식량 증가
# 변경점: teacher gpt2→gpt2-medium (124M→355M)
# T/α는 exp03 결과 기반으로 T=2.0, α=0.5 유지

teacher_model: gpt2-medium
student_model: distilgpt2

max_seq_length: 256
temperature: 2.0
alpha: 0.5

epochs: 3
batch_size: 4
learning_rate: 5.0e-5
