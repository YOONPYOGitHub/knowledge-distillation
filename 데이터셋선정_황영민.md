# [프로젝트 제안서] 온디바이스 실버케어 서비스 데이터 전략 및 검토서

## 1. 데이터셋 선정 및 학습 전략 개요
본 프로젝트는 자원이 극도로 제한된 **온디바이스(On-Device)** 하드웨어 환경에서 독거노인 정서 케어 및 라이프 로그 시스템을 **독립형(Standalone)**으로 구축하는 것을 목표로 합니다.

이를 위해 본 팀은 **"기초 한국어 성능 및 상식이 검증된 국산 LLM을 Teacher 모델로 선정"**하고, AI 허브에서 확보한 **"실버케어 도메인 특화 한국어 텍스트 데이터셋"**을 집중 결합하여 **지식 증류(Knowledge Distillation)** 및 **가지치기(Pruning)**를 수행합니다. 불필요한 범용 영어 가중치 등은 Pruning으로 과감히 걷어내고, 어르신 돌봄에 필수적인 한국어 맥락 지식만을 고밀도로 압축 주입함으로써 **4GB 이하의 소형 언어 모델(SLM)**에서도 최적의 실버케어 성능을 방어하고자 합니다.

---

## 2. 활용 데이터셋별 상세 선정 당위성 (AI 허브 실존 데이터 매핑)

### ① 자유대화 음성(노인남여) 텍스트 전사 데이터
* **선정 당위성 (노인 어조 및 맥락 이해):**
  * 기존 범용 대화 모델은 청장년층 중심의 텍스트로 구성되어 있어, 노년층 특유의 느린 대화 흐름, 반복적 표현, 구어체 맥락을 오해할 확률이 높습니다.
  * 본 데이터셋의 **텍스트 전사(Text) 데이터**를 학습시켜, 소형 모델이 노인 계층의 실제 일상 대화 패턴과 문장 구조를 완벽하게 이해하고 소통할 수 있는 **'어르신 맞춤형 페르소나'**를 구축합니다.

### ② 감성 대화 말뭉치
* **선정 당위성 (대화 속 감정 분류):**
  * 독거노인 돌봄의 핵심은 대화 속에서 우울증, 고독감, 불안감 등 이상 징후를 선제적으로 감지하는 것입니다.
  * 본 데이터셋에 포함된 **다차원 감정 라벨 텍스트**를 학습함으로써, 기기가 어르신의 슬픔이나 상처를 문맥 안에서 정확히 분류(Classification)하게 만듭니다. 이는 외부 서버 전송 없이 기기 내부 로컬 영역에 안전하게 **'라이프 로그(Life Log)'**를 생성하기 위한 핵심 감정 인지 백본이 됩니다.

### ③ 심리상담 데이터
* **선정 당위성 (전문적 정서 케어 및 답변 생성):**
  * 어르신의 감정을 진단하는 것을 넘어, 실제 심리치료적 효과가 있는 경청과 지지적 반응을 출력하기 위함입니다.
  * 전문 상담사의 대화 기술이 반영된 **정제된 대화 쌍(Q&A) 텍스트**를 주입하여, 무기력함이나 우울증을 호소하는 어르신에게 단순 반복형 답변이 아닌 따뜻하고 전문적인 위로 문장을 생성하는 페르소나를 완성합니다.

### ④ 중·노년층 한국어 방언 데이터 (강원도, 경상도 등)
* **선정 당위성 (입력 데이터의 강건성 확보):**
  * 실제 돌봄 서비스가 가장 시급한 지방 거주 독거노인 계층은 강력한 지역 방언과 억양 표현을 주로 사용하십니다.
  * 본 시스템은 추후 전처리 단계에서 음성을 텍스트로 변환하는 STT(Speech-to-Text) 모듈과 결합할 예정입니다. STT를 거쳐 들어올 *"시방 고마 배가 아파가꼬..."* 같은 투박한 사투리 텍스트를 소형 모델이 오차 없이 인지할 수 있도록, **방언-표준어 매핑 텍스트**를 사전 학습하여 **입력 데이터에 대한 강건성(Robustness)**을 물리적으로 확보합니다.

### ⑤ 한국어 성능이 개선된 초거대AI 언어모델 개발 및 데이터
* **선정 당위성 (한국어 기초 성능 및 안전 가드레일 방어):**
  * 지식 증류와 가지치기를 거친 초소형 모델(SLM)은 지식 저장 공간의 축소로 인해 '한국어 문장 문법의 붕괴'나 '환각 현상(Hallucination)'에 매우 취약해집니다.
  * 정제된 고품질 한국어 성능 개선 데이터셋을 지식 증류 파이프라인의 베이스 연료로 활용함으로써, 모델이 압축되는 과정에서도 **한국어의 정확한 문장 구조, 문화적 맥락, 기본적인 상식**을 잃지 않도록 최종 답변의 안정성과 **안전 가드레일(Safety Guardrail)**을 견고히 다집니다.

---

## 3. 부록: 프로젝트 제안서 데이터 부문 질의응답(Q&A) 검토서

### Q1. 실제 서비스 환경은 음성(Voice) 기반일 텐데, 학습 단계를 텍스트(Text) 데이터로만 제한한 사유는 무엇인가?
* **답변:** 최종 서비스 모델은 사용자의 편의성을 고려하여 음성 인터페이스(Speech-to-Speech)로 작동할 예정입니다.
* 다만, 본 프로젝트의 핵심 타겟인 온디바이스(On-Device) 하드웨어는 연산 및 메모리 자원이 극도로 제한되어 있습니다. 대용량 음성 파형 데이터를 모델이 직접 처리하게 할 경우, 추론 지연(Latency) 및 시스템 병목이 발생합니다.
* 따라서 시스템 아키텍처를 모듈화하여 전·후처리 레이어에 가벼운 STT(Speech-to-Text) 및 TTS(Text-to-Speech) 모듈을 배치하고, **핵심이 되는 경량 LLM은 오직 '텍스트 문맥 이해 및 답변 생성'에만 모든 연산 리소스를 집중**시키고자 합니다. 이는 소형 엣지 디바이스에서 실시간성을 확보하기 위한 최적의 엔지니어링 선택입니다.

### Q2. 선정한 데이터셋 중 '음성 데이터'나 '방언 데이터'가 포함되어 있는데, 텍스트 중심 학습 전제와 모순되지 않는가?
* **답변:** 명칭상 '음성 데이터'로 분류되어 있으나, 해당 데이터셋들은 음성 파일과 함께 **정밀하게 받아 적은 '텍스트 전사(Text Transcription) 스크립트(JSON/CSV 형식)'를 포함**하고 있습니다.
* 본 팀이 학습 연료로 활용하는 것은 음성 원본 파형이 아닌, 어르신들의 실제 발화 패턴과 사투리 표현이 그대로 보존된 **'전사 텍스트 데이터'**입니다. 따라서 100% 텍스트 기반 학습이라는 본 프로젝트의 기술적 전제와 완벽히 부합합니다.

### Q3. ②번 '감성 대화 말뭉치'와 ③번 '심리상담 데이터'는 정서 케어 영역에서 데이터의 중복성이 발생하는 것 아닌가?
* **답변:** 두 데이터셋은 모델 학습의 목적과 아키텍처 상의 역할이 명확히 구분됩니다.
* **'감성 대화 말뭉치'**는 사용자의 일상 발화 텍스트를 분석하여 현재의 심리 상태(슬픔, 불안 등)를 정밀하게 판별하는 **'감정 분류(Classification) 능력'**을 훈련하는 데 사용됩니다. 이는 라이프 로그에 이상 징후를 기록하기 위한 백본이 됩니다.
* **'심리상담 데이터'**는 그렇게 분류된 독거노인의 부정적 감정에 대해 전문 상담사 수준의 공감과 지지적 반응을 출력하는 **'답변 생성(Generation) 능력'**을 내재화하는 데 활용됩니다.
* 요약하자면 **감성 대화는 '어르신의 상태 진단'**을 위해, **심리상담 데이터는 '적절한 정서적 처방(답변)'**을 위해 혼합 적용하는 것이므로 중복이 아닌 상호 보완적 관계입니다.

### Q4. 개발 파트(홍윤표 팀원)에서 현재 위키백과 데이터로 테스트 중인데, 실버케어 도메인 데이터의 실제 적용 시점과 로드맵은 어떻게 되는가?
* **답변:** 현재 위키백과 데이터를 활용하는 것은 설계된 **Knowledge Distillation(지식 증류) 파이프라인의 알고리즘 안정성 및 연산 플러그인이 정상 작동하는지 검증(Sanity Check)하는 프로토타입 단계**입니다.
* 파이프라인의 구조적 검증이 완료되는 즉시, 데이터 담당자가 확보 및 정제한 5종의 실버케어 특화 텍스트 데이터셋으로 교체 주입(Data Switching)할 예정입니다. 엔진 빌드 후 진짜 도메인 연료를 투입하는 순차적 로드맵으로 계획되어 있습니다.

### Q5. 소형 언어 모델(SLM)의 고유 한계인 환각 현상(Hallucination)으로 인해, 헬스케어 관련 오답변이 출력될 위험성은 어떻게 방어할 것인가?
* **답변:** 경량화 과정에서 지식 저장 공간이 축소되면 환각 현상이 심화될 수 있으며, 실버케어 도메인에서 이는 치명적인 리스크가 됩니다.
* 이를 방어하기 위해 본 팀은 **초기 지식 증류 모델(Teacher) 선정 단계부터 한국어 상식과 문법 규격이 검증된 국산 대형 모델을 채택**합니다.
* 이에 더해, ⑤번 '한국어 성능 개선 데이터' 및 전문가가 검수한 정제된 텍스트 쌍을 지식 증류 프로세스에 구속(Binding)시킴으로써, 모델이 압축되더라도 올바른 한국어 문장 구조와 건강 정보의 한계를 벗어나지 않도록 강력한 **안전 가드레일(Safety Guardrail)**을 구축할 계획입니다.
